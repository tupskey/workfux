
export const Auth = (state = {
                token: null,
                userId: null,
                isloading: true,
                error: null
}, action) => {
    switch(action.type) {
        

        default:
            return state;
    }
}